---
import ThemeSwitcher from "./ThemeSwitcher";
---

<header
  class="sticky top-0 z-50 flex justify-between bg-base-200 items-center p-4 md:p-5 text-base-content border-4 border-primary m-4 rounded-lg transition-all duration-300 md:flex-nowrap flex-nowrap"
>
  <!-- Logo Section -->
  <div class="flex flex-col flex-shrink-0 text-4xl md:text-5xl">
    <h1 class="font-bold text-4xl md:text-5xl">DETECT</h1>
    <span class="text-sm md:text-base">AAI Solution</span>
  </div>
  
  <!-- Navigation Section -->
  <div class="flex items-center gap-3 md:gap-5 flex-shrink-0 text-sm md:text-base">
    <ThemeSwitcher client:load />
    <a href="/login" class="starfunk btn btn-primary btn-wide rounded-full"
      >Login</a
    >
    <a
      href="/register"
      class="starfunk btn btn-primary btn-wide rounded-full hidden md:inline-block"
      >Sign Up</a
    >
  </div>
</header>

<style>
  .starfunk {
    background: linear-gradient(
      90deg,
      var(--color-primary),
      var(--color-secondary)
    );
    color: var(--color-primary-content);
    padding: 0.75rem 2rem;
    font-size: 1rem;
    font-weight: bold;
    border: none;
    cursor: pointer;
    transition: opacity 0.2s ease;
  }
  .starfunk:hover {
    opacity: 0.9;
  }

  /* Adjust header when Sign Up button is removed */
  @media (max-width: 768px) {
    header {
      padding: 5px 10px;
      justify-content: space-between;
      gap: 8px;
    }
    .starfunk {
      padding: 0.6rem 1.5rem;
      font-size: 0.9rem;
    }
  }
</style>
